# version: '3.8'

services:
  client:
    container_name: client
    build:
      context: ./client
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "5173:5173"
    networks:
      - pacs
    depends_on:
      - api

  api:
    container_name: api
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - MONGO_URL=mongodb+srv://debashispani61:Skull%403434@cluster0.mmncbxr.mongodb.net
      - DB_NAME=Pacs_Prod
      - JWT_SECRET=ifabiaviabfi73828g
      - PORT=5000
      - ORTHANC_URL=http://orthanc:8042
    volumes:
      - ./server:/app
      - /app/node_modules
    ports:
      - "5000:5000"
    networks:
      - pacs
    depends_on:
      - orthanc

  orthanc:
    container_name: orthanc
    image: jodogne/orthanc-plugins:latest  
    ports:
      - "8042:8042"  
      - "4242:4242"  
    volumes:
      - /home/aarush/docker_volumes/orthanc_data:/var/lib/orthanc/db
      - /home/aarush/docker_volumes/orthanc_index:/var/lib/orthanc/db-index
      - /home/aarush/docker_volumes/orthanc_logs:/var/log/orthanc 
      - ./orthanc_config.json:/etc/orthanc/orthanc.json
      - ./LuaScripts:/etc/orthanc/LuaScripts
    restart: unless-stopped
    networks:
      - pacs

volumes:
  orthanc_data:
#   minio-data:

networks:
  pacs:
    driver: bridge
