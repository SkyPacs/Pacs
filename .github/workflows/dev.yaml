name: Deploy to Staging

on:
  pull_request:
    branches:
      - dev
    types:
      - closed  

jobs:
  build:
    runs-on:
    steps:
      - name:
        run:
      - name:
        run: 
      - name: Login to Container Registry
        run:
      - name: Build and Push Server Image
        run: 


  deploy:
    runs-on:
    steps:
